server:
  port: 8000

spring :
  cloud:
      gateway:
        routes:
          - id: user-service-id
            uri: lb://user-service
            predicates:
              - Path=/user-managment/**
            filters:
              - JwtCustomFilter

          - id: quizz-service-id
            uri: lb://quizz-service
            predicates:
              - Path=/quizz-managment/**

management:
  endpoint:
    gateway:
      enabled: true
  endpoints:
    web:
      exposure:
        include: gateway